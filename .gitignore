# Tauri / Rust build artifacts
target/
src-tauri/target/

# Ejecutable compilado — se distribuye solo vía GitHub Releases, nunca desde el repo
/dbv-md-reader.exe

# Node dependencies & dist
node_modules/
dist/

# OS temporary files
.DS_Store
Thumbs.db
desktop.ini

# Environment / IDE settings
.env
.env.local
.vscode/
.idea/
.claude/

# Contenido temporal usado para generar capturas de la landing page
.demo-tmp/

# Manifiesto del updater (RF-13) — se genera con scripts/generate-latest-json.mjs
# y se sube como asset de la Release de GitHub, nunca se commitea.
/latest.json
