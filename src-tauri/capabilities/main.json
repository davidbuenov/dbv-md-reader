{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permisos principales de la aplicación",
  "windows": ["main", "doc-*"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "shell:allow-open",
    "updater:default",
    "process:allow-restart"
  ]
}
