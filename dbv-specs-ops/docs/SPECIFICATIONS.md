# 📋 Especificaciones Técnicas (PRD): dbv-md-reader

> **Fase:** `/spec` (Especificación)  
> **Estado:** Validado  
> **Última Revisión:** 2026-09-02  

---

## 🎯 1. Contexto y Objetivos

- **Problema:** Los visores actuales de Markdown para Windows suelen estar basados en Electron (pesados, alto consumo de RAM > 200 MB, empaquetan Chromium completo) o requieren abrir un IDE (como VS Code) o navegadores web con extensiones. Falta una utilidad nativa, rápida y ligera dedicada exclusivamente a la **lectura de archivos `.md`** locales y remotos.
- **Objetivo (Éxito):** Crear una aplicación nativa para Windows (**dbv-md-reader**) enfocada en la lectura fluida de archivos Markdown, con consumo de RAM menor a 64 MB, instalador inferior a 8 MB, renderizado instantáneo (< 200 ms), auto-recarga en vivo y sanitización estricta de HTML mediante DOMPurify (JS, ver RF-03), lista para sustituir al Bloc de Notas de Windows como visor predeterminado para la extensión `.md`.  
  **Ampliación de alcance (desde v0.11.0, revertida la exclusión previa de edición — ver ADR-027 en `memory.md`):** el proyecto pasa de ser estrictamente de solo lectura a incorporar además un modo de edición ligera (RF-20/RF-21), sin renunciar a los presupuestos de tamaño de instalador y RAM anteriores — la decisión técnica es reutilizar el pipeline de renderizado ya existente y un `<textarea>` plano en vez de una librería de editor de código, precisamente para no comprometerlos.  
  **Ampliación de alcance a Android (decidida 2026-09-02, ver ADR-031 en `memory.md`):** tras un spike técnico de viabilidad (`tauri android init`, ver `task.md`), se inicia el `/spec` de una versión Android del lector — Android se aborda antes que iOS por decisión ya tomada con el usuario (iOS exige Mac + cuenta Apple Developer de pago). El MVP de Android es **solo lectura** (RF-20/RF-21 quedan fuera de alcance en esta fase, ver sección 6): coherente con el objetivo original de la app previo a la ampliación de v0.11.0, y evita resolver escritura persistente sobre Storage Access Framework (SAF) — el modelo de archivos de Android, sin rutas de sistema de archivos libres fuera del sandbox de la app — en esta primera versión. Se distribuye desde el primer lanzamiento vía **Google Play Store** (decisión explícita del usuario, no solo GitHub Releases/sideload como se validó primero en Linux), lo que implica cuenta de desarrollador de Google Play y política de privacidad publicada — ver RNF-01.

---

## 👥 2. Usuarios y Escenarios

- **Perfil de Usuario:** Desarrolladores, estudiantes, profesores, escritores técnicos, investigadores y usuarios de Windows que necesitan abrir y leer rápidamente archivos `.md` sin editar ni sobrecargar el sistema.
- **Escenarios Clave:**
  - *Escenario A (Doble clic / Abrir con):* El usuario hace doble clic sobre un archivo `README.md` en el Explorador de Archivos de Windows. `dbv-md-reader` se inicia instantáneamente cargando y renderizando el documento.
  - *Escenario B (Cursos y Documentación Interconectada):* El usuario sigue un curso o documentación modular en Markdown. Hace clic en un enlace a otro `.md` local (ej. `[Lección 2](./leccion2.md)`), el lector lo abre al instante y permite volver atrás con la flecha o `Alt + ←`.
  - *Escenario C (Vista previa en tiempo real / Auto-reload):* El usuario edita el documento en su editor preferido y `dbv-md-reader` actualiza automáticamente la vista en pantalla al guardar los cambios en disco.
  - *Escenario D (Diagramas Mermaid):* El usuario visualiza documentación técnica con diagramas de flujo o arquitectura incrustados (` ```mermaid `) que se renderizan como SVGs interactivos.
  - *Escenario E (Lectura prolongada y navegación):* El usuario conmuta entre temas (Claro, Oscuro, Sepia), navega con la Tabla de Contenidos (TOC) y busca texto con `Ctrl + F`.
  - *Escenario F (Edición ligera y vista dividida en vivo, añadido 2026-08-18):* El usuario activa el modo edición (`Ctrl + E`) sobre un documento ya abierto y ve el Markdown crudo a la izquierda y la vista renderizada a la derecha, actualizándose mientras escribe. Si en vez de escribir dentro de la app edita el mismo archivo desde otro editor externo (VS Code, Notepad++, Obsidian...), ambos paneles se actualizan solos en tiempo real mientras no tenga cambios propios sin guardar — útil como visor en vivo de código fuente + resultado sin necesidad de tocar el editor integrado.
  - *Escenario G (Documentación modular en carpetas, añadido 2026-08-20):* El usuario abre un archivo `.md` que forma parte de una carpeta más grande de documentación (un repo con `docs/`, un curso con varias lecciones). En vez de depender solo de enlaces internos (RF-08A) o del panel de Recientes (RF-11) para saltar a otro archivo de la misma carpeta, consulta el árbol de directorios en la pestaña "Archivos" del panel lateral, o pulsa `Ctrl + K` y teclea parte del nombre del archivo que busca. Un clic normal (o Enter en el Quick Open) abre el archivo en la misma ventana; `Ctrl/Cmd + clic` (o `Ctrl/Cmd + Enter`) lo abre en una ventana nueva sin perder el documento actual de vista.
  - *Escenario H (Lectura en Android, añadido 2026-09-02):* El usuario instala `dbv-md-reader` desde Google Play en su teléfono o tablet Android. Concede acceso a una carpeta completa mediante el selector nativo del sistema (Storage Access Framework), o abre un `.md` suelto desde su gestor de archivos/Gmail/Drive vía "Abrir con". La app renderiza el documento con el mismo pipeline que en escritorio (Markdown, Mermaid, KaTeX, temas, TOC) y permite navegar el árbol de la carpeta concedida (RF-25) y buscar archivos con Quick Open (RF-26) dentro de una única pantalla — sin ventanas adicionales, sin edición y sin recarga automática al modificar el archivo desde otra app.

---

## ✨ 3. Requisitos Funcionales (MVP & Mejoras)

- [x] **RF-01: Apertura mediante Argumento CLI / "Abrir con":**  
  La aplicación acepta una ruta de archivo como argumento al iniciarse (ej. `dbv-md-reader.exe C:\notas\readme.md`). Permite asociar el ejecutable como visor predeterminado en Windows mediante *"Abrir con..."*.  
  **macOS (añadido 2026-08-15):** Finder no lanza "Abrir con"/doble clic pasando la ruta como argumento de línea de comandos (a diferencia del Explorador de Windows) — envía un Apple Event `kAEOpenDocuments`, expuesto por Tauri v2 únicamente como `RunEvent::Opened { urls }`. La app escucha ese evento (orden `Opened → Ready → Window`, antes de que exista ninguna ventana): en arranque en frío guarda la ruta en estado gestionado (`OpenedFileState`) hasta que el frontend la recoge vía `get_cli_argument`; con la app ya en marcha reutiliza el mismo flujo de apertura de ventana que RF-14. Corrige un bug reportado por un usuario real (v0.7.0): "Abrir con" lanzaba la app vacía en macOS.
  **Android (añadido 2026-09-02, corregido en `/plan` tras verificar el generado real):** no hay paso de ruta por argumento CLI ni Apple Event — la app recibe la apertura vía Intent (arranque en frío o Activity ya viva, modelo de actividad única sin CLI ni multiproceso). **El `<intent-filter>` no se escribe a mano**: `tauri android init`/`tauri android build` lo genera automáticamente en `AndroidManifest.xml` a partir del mismo bloque `bundle.fileAssociations` de `tauri.conf.json` que ya asocia `.md` en Windows/Linux (el `mimeType: "text/markdown"` que corrigió la asociación en Linux, PR#9) — verificado leyendo el manifiesto real generado por el spike (`ACTION_VIEW`/`SEND`/`SEND_MULTIPLE`, MIME `text/markdown` + `pathPattern` `.md`/`.markdown`). **Riesgo residual aceptado (ver Adversarial Review en `memory.md`):** algunos proveedores de contenido externos (adjuntos de Gmail, "Abrir con" de Drive) no fijan `text/markdown` como MIME type real y entregan `application/octet-stream` — a verificar en dispositivo real durante `/build`; si aparece el gap, se añade una regla adicional por `pathPattern` sin restricción de MIME, sin tocar `bundle.fileAssociations`. La ruta que llega es una URI `content://` de Storage Access Framework, no una ruta de sistema de archivos — ver Modelo de Archivos en RF-07/RF-08A/RF-25 y ARCHITECTURE.md §5.
- [x] **RF-02: Renderizado Completo e Híbrido:**  
  - **Markdown Estándar:** Soporte para títulos (`#`..`######`), listas, tablas, enlaces, imágenes y bloques de código con resaltado de sintaxis (Prism.js). ✅ Implementado (`markdown-it` + Prism.js).  
    **Cobertura de lenguajes (ampliado 2026-08-16):** el build de Prism.js vendorizado originalmente solo traía las gramáticas `markup`/`css`/`clike`/`javascript` (build "core" por defecto) — cualquier otro lenguaje (C/C++, Python, Rust, Bash, JSON, YAML, TypeScript, Go, Java, C#, SQL, TOML, Diff, Markdown, PowerShell, Docker, INI...) se mostraba sin colorear. Añadidos ~20 componentes más vendorizados manualmente en `src/vendor/` (mismo patrón sin bundler que el resto de librerías), cargados como `<script>` adicionales tras `prism.min.js` en `index.html`.
  - **HTML Integrado:** Las etiquetas HTML válidas dentro del documento se renderizan respetando el diseño web estándar. ✅ Implementado (`html: true` en `markdown-it`) — **sin sanitizar**, ver riesgo en RF-03.
  - **Diagramas Mermaid:** Los bloques de código identificados con ` ```mermaid ` son procesados e inyectados como gráficos vectoriales (SVG). ✅ Implementado.  
    **Error de sintaxis compacto (corregido 2026-08-18):** un diagrama con error de sintaxis mostraba el SVG de error por defecto de Mermaid.js (icono de bomba grande, sin detalle útil). `mermaid.initialize({ ..., suppressErrorRendering: true })` hace que `render()` rechace la promesa en vez de resolver con ese SVG, y la app muestra un aviso propio compacto con el mensaje real del parser (línea y token exactos donde aplica). Bug detectado por el usuario con un ejemplo de Mermaid inválido en `src/markdownhelp_es.md` (RF-22).
  - **Cobertura GFM (GitHub Flavored Markdown, ampliado 2026-08-17):** `markdown-it` cubre CommonMark + tablas, ~~tachado~~, autolinks y HTML embebido (extensiones GFM incluidas de serie, sin plugins). **Listas de tareas** `- [ ]`/`- [x]` ✅ vía `markdown-it-task-lists` (checkboxes con `disabled=""`, **no interactivos** — coherente con la app de solo lectura, ver Fuera de Alcance). **Notas al pie** `[^1]` ✅ vía `markdown-it-footnote` (referencia numerada + sección de notas al final + retroenlace). Ambos plugins vendorizados en `src/vendor/`, sin CDN. Verificado 2026-08-17 con `testfiles/GFM_test.md` (fichero de pruebas de 57 secciones creado por el usuario).  
    **Alertas / callouts** `> [!NOTE]`/`[!TIP]`/`[!IMPORTANT]`/`[!WARNING]`/`[!CAUTION]` ✅ (añadido 2026-08-20): plugin propio sobre `md.core.ruler` (sin librería nueva) que reescribe la cita como un `div.markdown-alert` con icono SVG y color por tipo, coherentes en los tres temas. La sintaxis ya estaba documentada en la ayuda integrada desde antes, pero nunca se había implementado — el usuario lo detectó comparando el renderizado con el de GitHub.com.
- [x] **RF-03: Sanitización de HTML y Seguridad Estricta:**  
  Cualquier etiqueta o atributo HTML peligroso que intente ejecutar scripts (`<script>`, `onclick`, `onload`, `javascript:`, etc.) se elimina antes de insertarse en el DOM del WebView2. Implementado con **DOMPurify** (ver ADR-009 en `memory.md`) sobre el HTML ya renderizado por `markdown-it`, en vez de `ammonia` en Rust sobre el Markdown crudo (que corrompería bloques de código con `<`/`&`). ✅ Verificado 2026-08-09: `<script>` y `onerror` no se ejecutan; un bloque de código con `if (a < b)` se muestra intacto.
- [x] **RF-04: Experiencia de Lectura Despejada y Navegación:**  
  - Interfaz minimalista sin barras de herramientas pesadas. ✅  
  - Barra lateral flotante o colapsable con Tabla de Contenidos (TOC) generada automáticamente a partir de encabezados (`#`, `##`, `###`). ✅  
  - Atajo de teclado (`Ctrl + F`) para abrir una barra de búsqueda interna de texto rápida. ✅  
  - **TOC con sección activa, tiempo de lectura y barra de progreso (añadido 2026-08-16):** la Tabla de Contenidos resalta el encabezado actualmente visible mientras se hace scroll (`IntersectionObserver` sobre el contenedor de lectura, sin listener manual de `scroll`). Junto al nombre del documento se muestra el tiempo de lectura estimado (200 palabras/min sobre el markdown crudo). Una barra fina en el borde inferior de la cabecera indica el progreso de scroll del documento actual. Los tres se recalculan/reinician en cada carga de documento (apertura, navegación, auto-recarga).
  - **Ids de encabezado con slug estilo GitHub (corregido 2026-08-18):** `markdown-it` no genera ningún `id` de encabezado por defecto — `buildToc()` asignaba uno sintético (`h-0`, `h-1`...) basado solo en el orden del documento. Cualquier documento con una tabla de contenidos escrita a mano al estilo GitHub (`[Encabezados](#1-encabezados)`, patrón habitual en READMEs) tenía sus enlaces internos rotos, porque el `id` real nunca coincidía con el slug al que apuntaban. Corregido generando el mismo slug que usa GitHub (minúsculas, fuera puntuación/emoji, espacios→guiones, sufijo `-1`/`-2`... ante duplicados) — sin librería nueva, función propia en `app.js` (`assignHeadingIds()`, aplicada a h1-h6 de cualquier contenido renderizado, no solo a los h1-h3 que entran en la Tabla de Contenidos). Afecta también a los enlaces de ancla entre documentos (`archivo.md#seccion`, RF-08A), que comparten el mismo mecanismo de resolución. Bug real encontrado por el usuario probando `src/markdownhelp_es.md` (RF-22) como documento normal.  
    **Dos bugs más, mismo mecanismo, encontrados por el usuario 2026-08-20:** (1) el slug propio colapsaba guiones consecutivos y recortaba espacios sobrantes al quitar un emoji — GitHub no hace ninguna de las dos cosas (`githubSlug()` corregido para igualar el comportamiento real, verificado renderizando `markdownhelp_{es,en}.md`/`README*.md` con `markdown-it` real y comparando cada enlace del índice contra el id que generaría). (2) el navegador percent-codifica el `href` de un enlace `#ancla-con-tilde` (`é` → `%C3%A9`) al leerlo con `getAttribute('href')`, pero el `id` real del encabezado se queda en Unicode literal — `decodeAnchor()` nuevo en `interceptLinks()` decodifica el fragmento antes de comparar, en los 3 puntos donde se resuelve una ancla.
  - **Navegación por clic (TOC, enlaces internos, búsqueda) desalineada en Modo Edición (corregido 2026-08-18):** un clic en la Tabla de Contenidos dejaba la primera línea visible del panel de código una línea por debajo del encabezado real (la preview sí acertaba). Causa: `Element.scrollIntoView()` recorre y desplaza *todos* los ancestros scrollables del objetivo, y en Modo Edición `#reader-container` (con `overflow:hidden`, para que el código y la preview escroleen cada uno por su lado) sigue siendo un contenedor scrollable a efectos de script, así que también se movía él, descuadrando el cálculo. Sustituido por `scrollElementIntoView()`, que localiza el único contenedor que de verdad hace scroll (`findScrollHost()`, recorriendo `overflow-y` real en vez de asumirlo) y fija su `scrollTop` de forma explícita — usado también por la búsqueda interna (`Ctrl+F`) para el mismo motivo.
- [x] **RF-05: Modos Visuales (Temas):**  
  Soporte para tres temas básicos: Claro (estilo GitHub), Oscuro (estilo VS Code / GitHub Dark) y Sepia (lectura prolongada). ✅ Implementado con persistencia en `localStorage`.  
  **Colores de sintaxis por tema (añadido 2026-08-16, cierra deuda técnica):** el resaltado de código usaba siempre una hoja de estilos de Prism.js fija pensada para tema oscuro, así que en Claro/Sepia el texto quedaba con muy poco contraste. Sustituida por variables CSS propias por tema (`--code-keyword`, `--code-string`, `--code-comment`, `--code-function`, `--code-number`, `--code-tag`, `--code-class`, `--code-operator`) mapeadas a los tokens de Prism.js — el tema Oscuro conserva el aspecto anterior, Claro y Sepia pasan a tener contraste correcto. Ver ADR-022 en `memory.md`.
- [x] **RF-06: Auto-Reload por Modificación Externa (File Watcher):**  
  Integración de un observador de archivos en Rust (`notify` crate, comando `watch_file`, vigila el directorio padre — ADR-010). Si el archivo abierto es editado y guardado por otra aplicación, la vista de `dbv-md-reader` se recarga automáticamente en caliente sin perder la posición del scroll (evento `file-changed` + debounce en `app.js`). ✅ Verificado 2026-08-09: al añadir una sección al `.md` abierto, la Tabla de Contenidos se actualizó sola sin recarga manual.  
  **Fuera de alcance en Android (decidido 2026-09-02, ver ADR-031 en `memory.md`):** el crate `notify` no observa de forma fiable URIs `content://` de Storage Access Framework — no hay recarga automática al modificar el archivo desde otra app en Android. El usuario debe cerrar y reabrir el documento para ver cambios externos, documentado como limitación de plataforma (mismo criterio ya aplicado a la falta de auto-actualización de RF-13 en Linux, ADR-019).
- [x] **RF-07: Carga y Resolución de Imágenes Locales:**  
  Conversión automática de rutas relativas de imágenes (`./assets/imagen.png`) y rutas locales absolutas al protocolo de activos de Tauri (`asset://` vía `convertFileSrc` + `app.security.assetProtocol` en `tauri.conf.json`) para prevenir enlaces rotos por políticas de origen cruzado en WebView2. ✅ Verificado 2026-08-09: imagen relativa renderizada correctamente (bug original reportado por el usuario, corregido).  
  **Android (añadido 2026-09-02):** sin protocolo `asset://`/`convertFileSrc` de Tauri sobre URIs `content://` — la resolución de imágenes relativas dentro de la carpeta concedida vía SAF localiza el documento hijo por nombre relativo con `DocumentsContract.buildDocumentUriUsingTree()` y expone su contenido al WebView (vía un manejador de protocolo Tauri propio o conversión a `blob:`/`data:` con `ContentResolver.openInputStream()`) — implementación concreta a definir en `/plan`, ver ARCHITECTURE.md §5.
- [x] **RF-08A: Navegación de Documentos Markdown (Locales y Remotos):**  
  Si un enlace apunta a otro archivo Markdown (ej. `./leccion2.md` o un URL remoto `https://.../doc.md`), `dbv-md-reader` lo lee/descarga, lo sanitiza y lo abre dentro de la propia aplicación permitiendo navegar por la estructura del curso o documentación. Los enlaces `.md` locales relativos funcionan (`resolve_relative_path` + `read_file`). La descarga de `.md` remotos está implementada con `ureq` (`read_file` detecta `http(s)://` y descarga; `resolve_relative_path` une URLs cuando la base es remota). ✅ Verificado 2026-08-09 con un test de integración (`read_file_downloads_remote_markdown`, `#[ignore]` por defecto — requiere red) contra una URL real (`raw.githubusercontent.com`): descarga, `file_name` y `dir_path` correctos.  
  **Entrada de UI (RF-08A-UI, añadido 2026-08-09):** hasta ahora solo se podía llegar a un `.md` remoto haciendo clic en un enlace *dentro* de un documento ya abierto — no había forma de pegar una URL directamente (el selector nativo de archivos no admite `http(s)://`). Se añadió un botón "Abrir desde URL" en la barra superior y un enlace "o abrir desde una URL" en el Estado Vacío (RF-09), que despliegan un panel con un campo de texto; al confirmar (botón "Abrir" o `Enter`) se invoca la misma ruta de carga que el resto de aperturas explícitas (`isPrimaryOpen`, se registra en Archivos Recientes). ✅ Verificado con una URL real del usuario.  
  **Android (añadido 2026-09-02):** la navegación entre `.md` locales enlazados dentro de la misma carpeta se resuelve igual que las imágenes de RF-07 (SAF, `DocumentsContract`), no con `resolve_relative_path` sobre rutas de sistema de archivos. La descarga de `.md` remotos (`ureq`) no depende del sistema de archivos y funciona igual que en escritorio.
- [x] **RF-08B: Historial de Navegación (Atrás / Adelante):**  
  Mantiene una pila de historial de documentos leídos con botones discretos `←` / `→` en la barra superior y atajos universales `Alt + Left` (Atrás) / `Alt + Right` (Adelante) para regresar al documento anterior sin perderse en el curso. ✅ Implementado.
- [x] **RF-08C: Enlaces a Páginas Web Externas:**  
  Si un enlace apunta a una página web general (ej. `https://github.com`), se abre directamente en el navegador predeterminado del sistema operativo para mantener la aplicación ligera y segura. ✅ Implementado (`tauri-plugin-shell`).
- [x] **RF-09: Estado "Sin Archivo Abierto" (Empty State & Drag and Drop):**  
  Si la aplicación se ejecuta sin argumentos CLI, muestra una pantalla limpia con zona para arrastrar y soltar (Drag & Drop) un archivo `.md` y acceso al selector de archivos (`Ctrl + O`). ✅ Implementado.
- [x] **RF-10: Utilerías de Lectura (Copiar Código, Zoom, Exportar PDF):**  
  - Botón disimulado "Copy" en bloques de código. ✅  
  - Zoom de lectura con `Ctrl + +`, `Ctrl + -` y `Ctrl + 0`. ✅ El zoom se aplica tanto al contenido (`#content`) como a la Tabla de Contenidos (`#toc-sidebar`) — corregido 2026-08-09 (antes solo afectaba al contenido).  
  - Impresión / Exportación a PDF mediante `Ctrl + P`. ✅ El `@media print` controla paginación real (`@page` con tamaño A4 y márgenes, evita títulos/tablas/imágenes/diagramas/fórmulas partidos entre páginas, muestra la URL junto a cada enlace) en vez de solo ocultar cabecera/paneles — actualizado 2026-08-12. Sigue siendo el diálogo de impresión nativo del sistema operativo (`window.print()`), no una exportación sin diálogo (ver RF-18 en el roadmap, fuera de alcance de esta fase). **Revisado 2026-08-16:** confirmado que ese alcance se mantiene conscientemente (RF-18 nativo sigue fuera, ver ADR-022) — no fue necesario tocar código, el tooltip ya mencionaba "PDF".
  - **Números de línea y ajuste de línea (wrap) en bloques de código (añadido 2026-08-16):** todo bloque de código muestra numeración de línea (plugin oficial `prism-line-numbers`, vendorizado). Un botón "Wrap line" junto al de copiar alterna entre el scroll horizontal por defecto y el ajuste de línea, para líneas muy largas.  
    **🔧 Corregido (ciclo v0.16.0) — numeración de línea desalineada (dos causas, una de ellas distinta de la hipótesis inicial):** los usuarios reportaron que los números no coinciden con su línea de código. La hipótesis del `/spec` (limitación de `prism-line-numbers` con `white-space: pre-wrap`) resultó **incompleta y no ser la causa principal** — medido con un banco de pruebas en Edge/Chromium headless (mismo motor que WebView2) sobre el CSS y el DOM reales, antes y después del fix:
      1. **Causa principal (presente siempre, también SIN Wrap):** `.markdown-body pre code { font-size: 0.875em }` reducía la fuente solo en el `<code>`, mientras el interlineado (`line-height: 1.75` de `.markdown-body`) era *unitless* y por tanto cada elemento lo recalculaba sobre su propia fuente. La altura real de cada línea de código la marca el *strut* del `<pre>` (1.75 × 15px = **26.25px**), pero los números —que viven en un `<span>` absoluto dentro del `<code>`— median 1.75 × 13.125px = **22.97px**: una deriva acumulativa de **−3.28px por línea** (−39px en la línea 13). Corregido moviendo el `font-size: 0.875em` del `<code>` al `<pre>` y declarando `line-height: 2em` (longitud, se hereda ya resuelta, idéntica para ambos). Los valores finales (13.125px de fuente, 26.25px de interlineado) son exactamente los de antes del fix: el bloque de código no cambia de aspecto, solo se alinean los números.
      2. **Causa secundaria (solo con Wrap activado):** el botón "Wrap line" cambia `white-space` a `pre-wrap`, y una línea larga pasa a ocupar varias líneas visuales sin que la numeración lo tuviera en cuenta.
      **Cambio de mecanismo (decisión tomada tras dos intentos fallidos, ver ADR-046 en `memory.md`):** se retira el plugin `prism-line-numbers` y con él todo el modelo de "columna paralela de números cuyas alturas se sincronizan por JavaScript". Los dos intentos de arreglarlo por medición (primero la API `resize()` del propio plugin, luego una medición propia con `Range`) taparon unos casos y dejaron otros, porque una altura estimada o medida a posteriori nunca queda garantizada frente a ajuste de línea, zoom, fuentes o re-layouts posteriores. Ahora `applyLineNumbers()` (`app.js`) reparte el HTML ya resaltado por Prism en **un elemento `.code-line` por línea lógica** y el número se pinta con `::before` en el gutter de esa misma caja de línea (`styles.css`), de forma que **la alineación la garantiza el navegador y no hay nada que sincronizar**: al envolver, la caja de la línea crece y su número la acompaña solo. Es el modelo de una fila por línea que usa GitHub. El troceado reabre clonados los tokens que abarcan varias líneas (comentarios de bloque, plantillas de cadena) para no perder su color.
      Verificado con un banco de medición en Edge/Chromium headless (mismo motor que WebView2) sobre el CSS y el código reales: contenido reconstruido **idéntico** al original (sin corrupción al trocear tokens), un `.code-line` por línea lógica, comentario de bloque de 3 líneas conservando color en las 3, líneas vacías con su altura correcta (26.25px, no colapsadas), y con Wrap la línea larga creciendo a exactamente 3 × 26.25px con la siguiente línea arrancando justo ahí — **sin JavaScript de sincronización**. Medidas del mecanismo anterior, para contraste: −39px de desviación sin Wrap y −92px con Wrap en un bloque de 13 líneas.  
    **🔧 Corregido (ciclo v0.16.0) — pérdida de color de sintaxis al imprimir/exportar a PDF:** `@media print { .token { color: inherit !important; text-shadow: none !important; } }` (`styles.css`, añadido en la Fase 27 para forzar contraste en papel) anulaba **todos** los colores de resaltado de sintaxis de golpe en vez de solo ajustar los que tuvieran bajo contraste — el código salía en un único color grisáceo ("todo gris", en palabras del usuario). Corregido eliminando esa regla y forzando en su lugar, dentro de `@media print`, la paleta de variables `--code-*`/`--text-secondary` del tema Claro (alto contraste en fondo blanco, misma paleta que ya usa RF-05 en pantalla) independientemente del tema activo en pantalla (Oscuro/Sepia incluidos) — así el código impreso nunca hereda colores pensados para fondo oscuro. Se añade también `print-color-adjust: exact` / `-webkit-print-color-adjust: exact` para que WebView2/WebKitGTK/WKWebView respeten esos colores en la vista previa e impresión reales, sin depender de que el usuario marque "gráficos de fondo" en el diálogo nativo del SO.  
    **🔍 En investigación (ciclo v0.16.0) — Exportar PDF dedicado sin diálogo nativo, ver RF-28.**
- [x] **RF-12: "Acerca de":**  
  Botón en la barra superior que abre un panel modal con el nombre de la aplicación, la versión actual (leída dinámicamente del backend Rust vía `get_app_version`, sincronizada siempre con `Cargo.toml`), enlaces a la web del autor (`davidbuenov.com`) y su GitHub (`github.com/davidbuenov`) abiertos en el navegador del sistema, y la licencia. ✅ Implementado y verificado 2026-08-09.
- [x] **RF-11: Archivos Recientes (Recent Files):**  
  - El backend de Rust persiste en disco (directorio de datos de la aplicación, `recent_files.json`) una lista de hasta 10 rutas de archivos `.md` abiertos explícitamente (CLI, diálogo nativo o Drag & Drop), con nombre de fichero y fecha/hora de última apertura.  
  - Un botón "Recientes" en la barra superior despliega un panel con esa lista; al hacer clic en una entrada se reabre el documento y sube al principio de la lista.  
  - Si un archivo recientemente registrado ya no existe en disco (movido/borrado), se descarta silenciosamente de la lista al mostrarla o al intentar abrirlo, sin bloquear la aplicación.  
  - El Estado "Sin Archivo Abierto" (RF-09) muestra también un acceso directo a los últimos archivos recientes (si existen) para evitar tener que buscar el fichero de nuevo.  
  - La navegación interna entre documentos enlazados (RF-08A) y los botones Atrás/Adelante (RF-08B) **no** añaden entradas a esta lista — solo las aperturas explícitas cuentan como "recientes".  
  - Un botón "Limpiar historial" vacía la lista de recientes.
  - ✅ Verificado 2026-08-09: al abrir por CLI, `recent_files.json` registró correctamente la ruta, nombre y timestamp.
- [x] **RF-13: Comprobación de Actualizaciones (bajo demanda):**  
  - La aplicación **nunca** comprueba actualizaciones al arrancar — decisión consciente para no penalizar el arranque instantáneo (< 200 ms, RNF principal del proyecto).  
  - El panel "Acerca de" (RF-12) incluye un botón "Buscar actualizaciones" que, al pulsarlo, consulta el manifiesto `latest.json` publicado junto a cada Release de GitHub (`tauri-plugin-updater`).  
  - Si ya está actualizado: "Ya tienes la última versión.". Si hay una versión nueva: "Nueva versión X.Y.Z disponible." y el botón cambia a "Actualizar"; al pulsarlo, descarga e instala el nuevo instalador NSIS en segundo plano y relanza la aplicación (`tauri-plugin-process`, `relaunch()`).  
  - Los paquetes de actualización van firmados (par de claves `minisign` generado con `tauri signer generate`, clave pública embebida en `tauri.conf.json` → `plugins.updater.pubkey`); un paquete sin firma válida o de un origen distinto no se instala.  
  - Errores de red o de instalación se muestran como texto discreto en el propio panel, sin bloquear la aplicación ni mostrar diálogos nativos intrusivos (`plugins.updater.dialog: false`, UI 100% propia).
  - **Android (añadido 2026-09-02):** sin `tauri-plugin-updater` — Google Play gestiona la distribución de actualizaciones igual que cualquier otra app de la tienda. Mismo criterio ya aplicado a Linux en RNF-01: RF-13 queda fuera de alcance específico para esta plataforma, sustituido por el mecanismo nativo de la tienda.
- [x] **RF-14: Instancia única del proceso (multi-ventana, no multi-proceso):**  
  - Abrir un `.md` (doble clic, "Abrir con...", CLI) mientras `dbv-md-reader` ya está en marcha **no** lanza un proceso `dbv-md-reader.exe` nuevo — abre una ventana adicional dentro del **mismo** proceso (`tauri-plugin-single-instance`), visible como un único árbol en el Administrador de Tareas de Windows con varias ventanas, igual que Notepad++.  
  - Cada ventana mantiene su documento, zoom, TOC, búsqueda y observador de archivos completamente independientes de las demás — no se comparte estado entre ventanas, solo el proceso.  
  - Si la segunda apertura no trae ninguna ruta de archivo (p. ej. relanzar el `.exe` sin argumentos), no se abre una ventana vacía: se enfoca una ya existente.  
  - **Fuera de alcance de este RF** (decisión consciente, ver ADR-015 en `memory.md`): abrir un archivo *desde dentro* de la app (`Ctrl+O`, panel de Recientes, Drag & Drop sobre una ventana ya abierta) sigue sustituyendo el documento de esa misma ventana, sin crear una ventana nueva — solo las aperturas *externas* (Explorador de Windows) consolidan proceso.
  - **Primera excepción consciente a esta regla (añadida 2026-08-20, ver RF-25/RF-26):** el Explorador de árbol (RF-25) y el Quick Open (RF-26) son las dos primeras entradas *internas* que sí pueden abrir una ventana nueva — pero solo cuando el usuario lo pide explícitamente con `Ctrl/Cmd + clic` o `Ctrl/Cmd + Enter`. Un clic o Enter simple en cualquiera de los dos sigue sustituyendo el documento de la ventana actual, igual que el resto de entradas internas. Reutilizan el mismo `open_document_window`/`open_or_focus_document` ya existente (RF-14) — ninguna lógica de ventanas nueva, solo un nuevo punto de entrada consciente hacia código ya probado.
  - **Foco y deduplicación de ventanas (añadido 2026-08-15):** un bug reportado por un usuario real hacía que la ventana nueva no viniera al frente al reabrir un archivo con la app ya en marcha — el usuario, sin ver ningún cambio visible, repetía el doble clic y acumulaba varias ventanas con el mismo documento. Ahora: (1) toda ventana nueva o reutilizada se trae al frente (`unminimize` + `set_focus`); (2) si el archivo que se pide abrir ya se está mostrando en una ventana viva de este mismo proceso (`OpenDocumentsState`, comparación por ruta canonicalizada), esa ventana se enfoca en vez de abrir un duplicado.
  - **Android (añadido 2026-09-02):** no aplica — el modelo de una sola Activity no tiene equivalente a "múltiples ventanas del mismo proceso", y `tauri-plugin-single-instance` no está disponible en esa plataforma (el propio sistema operativo ya garantiza una sola instancia de la Activity). Un nuevo Intent (`ACTION_VIEW` sobre otro `.md`, o navegación interna) sustituye el documento mostrado en la Activity actual — mismo comportamiento que ya tienen hoy las aperturas internas en escritorio (`Ctrl+O`, Recientes, Drag & Drop), nunca crea nada parecido a una ventana nueva.
- [x] **RF-15: Abrir un diagrama Mermaid en mermaid.live:**  
  - Menú contextual (botón derecho) sobre cualquier diagrama Mermaid ya renderizado, con la opción "Abrir en mermaid.live".  
  - Genera la URL del editor en línea codificando el código fuente del diagrama en el fragmento `#pako:...` (mismo formato que usa mermaid.live internamente para compartir enlaces — compresión `deflate` + Base64 URL-safe, sin ningún servidor intermedio) y la abre en el navegador predeterminado.  
  - Pensado para diagramas grandes/densos donde el zoom interno de la app (tope 200%, RF-10) no basta para leer el detalle — mermaid.live ofrece pan/zoom libre.
- [x] **RF-16: Interfaz en español e inglés:**  
  - Selector de idioma (ES/EN) en la barra superior, junto al selector de temas — mismo patrón visual, persistente en `localStorage`.  
  - Detección automática al primer arranque a partir del idioma del sistema (`navigator.language`); a partir de ahí prevalece la elección manual del usuario.  
  - Cubre toda la interfaz de la aplicación (barra de herramientas, paneles, mensajes de error, actualizaciones, tiempos relativos de "Recientes") mediante diccionarios en `src/i18n.js`, sin librería de i18n externa.  
  - **Fuera de alcance de este RF:** el instalador NSIS y la ficha de Microsoft Store no se traducen — siguen en español (instalador) e independientemente en cada idioma (ficha de Store, ver `descripcionStore_es.md` / `descripcionStore_en.md` en la raíz del proyecto). El contenido de los documentos `.md` que el usuario abre tampoco se traduce (no tendría sentido — es su propio contenido).
- [x] **RF-17: Renderizado de ecuaciones matemáticas (LaTeX):**  
  - Sintaxis soportada: inline `$...$`, bloque `$$...$$` y bloque de código ` ```math `. Delimitador de apertura sin espacio pegado, de cierre sin espacio precedente, `\$` escapa un símbolo de dólar literal (misma convención que Pandoc/GitHub/Obsidian) — colisión conocida y aceptada con precios (`$5 y $10`): si son cifras reales, se escriben con `\$`.  
  - Renderizado con **KaTeX** (vendorizado en `src/vendor/`, sin CDN, mismo patrón que Mermaid), síncrono y sin coste de arranque perceptible.  
  - Un `$...$`/`$$...$$` extraído del Markdown crudo antes del parseo (preprocesado, respetando bloques y spans de código) y sustituido por LaTeX renderizado tras el parseo (postprocesado, junto a los diagramas Mermaid) — necesario porque, a diferencia de un bloque ` ```mermaid `, la sintaxis `$...$` es texto normal dentro de un párrafo y las reglas `emphasis`/`typographer` de `markdown-it` podrían corromperla si se dejara pasar sin proteger (mismo tipo de problema de capa que RF-03, ver ADR-009).  
  - Un LaTeX mal formado en el documento del usuario no rompe el resto del render — se muestra el error en rojo in-place (`throwOnError: false`), coherente con la filosofía de solo lectura tolerante a documentos ajenos.  
  - Fuera de alcance: sintaxis alternativa `\(...\)`/`\[...\]` (tampoco la soportan GitHub/Pandoc por defecto).
- [x] **RF-19: Always on Top (ventana siempre visible):**  
  - Botón icono (chincheta) en la barra superior, junto a "Imprimir". Un clic activa, otro desactiva — sin menú ni opciones. El icono se resalta y el tooltip cambia (`toolbar.alwaysOnTop` / `toolbar.alwaysOnTopActive` en `src/i18n.js`) para reflejar el estado actual.  
  - Implementado con la API multiplataforma de Tauri (`getCurrentWindow().setAlwaysOnTop()`), sin comando Rust nuevo — solo requirió añadir el permiso `core:window:allow-set-always-on-top` en `src-tauri/capabilities/main.json` (`core:default` solo trae la lectura `allow-is-always-on-top`, no el mutador).  
  - **Por ventana, sin persistencia:** cada ventana (`main`, `doc-*`) es independiente en esto igual que ya lo es en zoom/TOC/búsqueda (RF-14) — arranca siempre apagado en cada apertura nueva, no se guarda en `localStorage` (las etiquetas `doc-0`/`doc-1`... no son estables entre relanzamientos, y es una decisión de sesión, no una preferencia duradera).  
  - Sin atajo de teclado (solo el botón), para no colisionar con los atajos ya existentes ni con el módulo homónimo de PowerToys (`Win+Ctrl+T`) si el usuario lo tiene instalado.  
  - ✅ Verificado 2026-08-17 en Windows: `WS_EX_TOPMOST` se activa/desactiva correctamente con cada clic (comprobado vía Win32, no solo visualmente), el botón/tooltip reflejan el estado, y una segunda ventana (`doc-*`) abierta con la primera ya fijada arranca sin heredar ese estado. **Multiplataforma por diseño** (la API de Tauri ya abstrae Windows/macOS/Linux, sin código condicional), pero macOS y Linux —especialmente Wayland, donde algunos compositores restringen que una app se autofije "siempre encima"— no se han podido probar en esta sesión (solo hay acceso a Windows); riesgo aceptado, ver `task.md`.
- [x] **RF-20: Modo Edición (Vista Dividida Fuente + Vista Previa), añadido 2026-08-18:**
  - Un botón/atajo (`Ctrl + E`) alterna entre el modo lectura actual (RF-02 a RF-05) y un modo edición: panel dividido con el Markdown en crudo a la izquierda y la vista renderizada a la derecha. El botón vive en la barra superior junto al de Tabla de Contenidos (RF-04), sin separador entre ambos, para señalar visualmente que los dos son paneles que se abren/cierran.
  - El panel de código es un `<textarea>` plano (sin resaltado de sintaxis ni componente de editor de código tipo CodeMirror/Monaco) — decisión consciente para no comprometer los Requisitos No Funcionales de tamaño de instalador y RAM (sección 5). Referencia de mercado: la app `READU.md` (WinUI 3, código abierto) resuelve su modo edición con el mismo patrón minimalista.
  - Cada pulsación de tecla dispara un re-render con debounce (~300-500 ms) sobre el mismo `renderMarkdown()` ya existente (RF-02), incluida la sanitización DOMPurify (RF-03) — el modo edición nunca inserta HTML sin sanitizar, ni añade un pipeline de renderizado nuevo.
  - **Vista Dividida en Vivo / Modo Espejo:** mientras el panel de código no tenga cambios sin guardar, la auto-recarga por cambios externos (RF-06) pasa a actualizar **ambos** paneles (código fuente y vista previa) en vez de solo el renderizado — ver Escenario F.
  - **Guardado (`Ctrl + S`):** escribe el contenido del panel de código en el archivo original mediante un nuevo comando Rust `write_file` (escritura directa con `fs::write`, mismo patrón sin plugin `fs` oficial de Tauri que ya usa `read_file`, RF-01/RF-08A). El guardado siempre sobreescribe el archivo abierto — sin diálogo de "Guardar como" en esta fase (ver sección 6). **Botón visible (corregido 2026-08-18):** el guardado solo estaba disponible por atajo de teclado, sin ningún control visual — un usuario real no encontraba cómo guardar. Añadido un botón "Guardar" (icono disquete) junto al de Modo Edición, visible solo mientras el modo edición está activo.
  - Fuera de alcance de este RF (ver sección 6): resaltado de sintaxis en el panel de edición, atajos de formato Markdown, deshacer/rehacer más allá del nativo del `<textarea>`.
  - **Fuera de alcance en Android en esta fase (decidido 2026-09-02, ver ADR-031 en `memory.md`):** el MVP de Android es de solo lectura — RF-20/RF-21 no se implementan en la primera versión de esta plataforma, evitando resolver permisos de escritura persistente sobre SAF (`ACTION_OPEN_DOCUMENT_TREE` + `takePersistableUriPermission`) y una gestión de conflictos sin file watcher fiable (ver RF-06 en Android). Candidato a una fase posterior con su propio `/spec`.
  - **Números de línea (añadido 2026-08-18, pedido por el usuario):** columna de numeración junto al `<textarea>`, calculada a partir de los saltos de línea reales (`value.split('\n')`) y sincronizada con su `scrollTop` en cada `input`/`scroll` — sin librería de editor de código (coherente con la decisión de ADR-027). El `<textarea>` no hace ajuste de línea (`wrap="off"`, scroll horizontal en su lugar) para que cada línea lógica sea siempre una única línea visual y la numeración no se desalinee.
  - **Paneles redimensionables por arrastre (añadido 2026-08-18, pedido por el usuario):** divisor arrastrable entre el panel de código y la preview (fracción del ancho persistida en `localStorage`, reescala correctamente al cambiar el tamaño de ventana) y otro para la Tabla de Contenidos (ancho en píxeles persistido, RF-04) — sin librería de layout, listeners nativos `mousedown`/`mousemove`/`mouseup`.
  - **Sincronización de scroll editor↔preview↔TOC (añadido 2026-08-18, pedido por el usuario):** al hacer scroll en el panel de código, la preview se desplaza a la posición correspondiente, y viceversa — interpolación lineal entre encabezados (h1/h2/h3), usando la línea de origen real de cada uno (expuesta por `markdown-it` vía `token.map`) y su posición ya renderizada en la preview, no un simple porcentaje global (que se desalinearía en documentos con secciones de longitud desigual). La Tabla de Contenidos ya se mantenía sincronizada por el scrollspy existente (RF-04); se amplió para además desplazarse ella misma y mantener visible el enlace activo.
  - ✅ Verificado 2026-08-18 en Windows lanzando la app real (`tauri dev` + UI Automation + capturas): panel dividido correcto al activar `Ctrl+E`/botón, preview actualizada tras el debounce al escribir, contenido persistido en disco tras `Ctrl+S` (confirmado leyendo el archivo, no solo por la UI), numeración de línea alineada y sincronizada al hacer scroll en un documento de 527 líneas. **Arrastre de los divisores y sincronización de scroll verificados manualmente por el usuario** (la simulación de arrastre por coordenadas de ratón no es fiable contra WebView2 en este entorno, mismo motivo ya documentado en ADR-024/`memory.md` — no se insistió en automatizarlo).
- [x] **RF-21: Gestión de Conflictos entre Edición Local y Cambios Externos, añadido 2026-08-18:**
  - Si llega un evento de cambio externo (`file-changed`, RF-06) mientras el panel de código **no** tiene cambios sin guardar, se recarga en silencio — comportamiento ya existente de RF-06, ahora extendido también al panel de código por el Modo Espejo de RF-20.
  - Si llega un evento de cambio externo mientras el panel de código **sí** tiene cambios sin guardar, la primera vez se muestra un diálogo modal bloqueante con dos opciones: "Conservar mis cambios" o "Recargar desde disco". Patrón evaluado explícitamente frente a dos referencias reales (Notepad++, que siempre interrumpe incluso sin cambios propios, y el Bloc de Notas moderno de Windows, que solo interrumpe si hay cambios sin guardar) — se adopta el criterio del Bloc de Notas por menos intrusivo sin sacrificar seguridad frente a pérdida de datos.
  - Tras esa primera interrupción, si el usuario elige "Conservar mis cambios", no se repite el modal ante nuevos eventos del mismo conflicto — en su lugar aparece una franja no bloqueante y persistente ("El archivo cambió en disco desde tu última edición") con una acción "Recargar desde disco" siempre disponible, para no perder la vía de recarga manual solo por no repetir la interrupción.
  - El propio guardado (`Ctrl + S`, RF-20) no debe disparar ni el modal ni la franja contra el archivo que la propia aplicación acaba de escribir — se aplica una ventana de supresión (~800 ms) tras cada `write_file` para el mismo `path` antes de procesar el siguiente `file-changed`.
  - Fuera de alcance de este RF (ver sección 6): fusión automática (merge) de cambios divergentes, control de versiones/histórico dentro de la app, detección de conflictos por comparación de contenido más allá del propio evento del sistema de archivos.
  - **Multiplataforma explícito (Windows/Linux/macOS, RNF-01), pedido por el usuario 2026-08-18:** RF-20/RF-21 se diseñan sin código condicional por sistema operativo — `std::fs::write` (nuevo comando `write_file`) y el crate `notify` (RF-06) ya abstraen los 3 sistemas; los atajos `Ctrl+E`/`Ctrl+S` deben comprobar también `e.metaKey` para `Cmd+E`/`Cmd+S` en macOS (mismo patrón ya usado desde el PR#4, ver ADR-026 en `memory.md`). No verificado en Linux/macOS en esta sesión (solo Windows disponible) — ver tabla ampliada en `ARCHITECTURE.md` §5 y riesgo explícito en `implementation_plan.md`.
  - ✅ Verificado 2026-08-18 en Windows: guardado sin disparar el modal contra el propio guardado (bug real de carrera encontrado y corregido en esta misma sesión, ver `memory.md`), documento remoto (RF-08A) con el modo edición deshabilitado y una etiqueta "Solo lectura"/"Read-only" junto al nombre del documento (ES/EN).
  - **Indicador "Modificado" + confirmación al descartar cambios propios sin guardar (añadido 2026-08-20, gap real detectado por el usuario):** hasta ahora, cerrar la ventana, salir de Modo Edición o navegar a otro documento con cambios sin guardar los descartaba en silencio — el modal de conflicto de más arriba solo cubre cambios *externos* al fichero, no la pérdida de cambios *propios* al salir sin guardar. Etiqueta "Modificado" (mismo formato que "Solo lectura", `.header-badge`) visible junto al nombre del documento mientras `editMode && isDirty`. Un modal propio (mismo patrón `.modal-overlay`/`.modal-card` que el resto, no un diálogo nativo del sistema — ver ADR-030 en `memory.md`) pregunta antes de descartar en los tres casos (cerrar ventana, `Ctrl+E`, Atrás/Adelante/abrir otro documento), con las mismas dos opciones ("Seguir editando" / "Descartar cambios"). ✅ Verificado 2026-08-20 en Windows por el usuario en real, en los tres disparadores.
- [x] **RF-22: Ayuda de Sintaxis Markdown (chuleta ES/EN), añadido 2026-08-18:**
  - Botón "?" en la barra superior, junto al de "Acerca de" (RF-12) — abre un modal con una guía de referencia completa de sintaxis Markdown/GFM (encabezados, énfasis, listas, tablas, notas al pie, HTML embebido, fórmulas, Mermaid, etc.), pensada para quien usa el Modo Edición (RF-20) sin conocer bien la sintaxis.
  - Contenido en dos ficheros estáticos bundleados como assets del frontend (`src/markdownhelp_es.md` / `src/markdownhelp_en.md`, mismo patrón sin CDN/sin recurso de Tauri aparte que el resto de librerías vendorizadas) — se elige el fichero según el idioma activo de la interfaz (RF-16), no un selector aparte.
  - **Mismo pipeline de renderizado que el documento principal, sin excepción (corregido 2026-08-18):** la primera versión usaba un renderizador propio y más pobre (solo `md.render()` + Prism), sin ids de encabezado, sin interceptar enlaces y sin procesar Mermaid/KaTeX — un usuario real lo detectó ("parece un previsualizado diferente"). `renderMarkdown()` (RF-02/RF-03) se generalizó para aceptar un contenedor destino (`contentEl` por defecto, `#help-content` para este modal) y ejecutar exactamente los mismos pasos: sanitizado, resaltado Prism, ids de encabezado (`assignHeadingIds`, ver más abajo), Mermaid, KaTeX e interceptación de enlaces — solo la actualización de la TOC lateral, el tiempo de lectura, la barra de progreso y la resolución de imágenes relativas quedan reservadas al documento principal. La tipografía de prosa (`#content`) pasa a la clase compartida `.markdown-body`, aplicada también a `#help-content`, para que ambos se vean idénticos sin mantener dos hojas de estilos en paralelo.
  - Contenido obtenido vía `fetch()` relativo (el propio origen que sirve `index.html`) en vez de pasar por `read_file`/Rust — no es un documento del usuario, es contenido propio de la aplicación.
  - **Relectura en cada apertura, no solo la primera (corregido 2026-08-20):** la versión anterior cacheaba en memoria tras la primera carga por idioma y nunca volvía a leer el fichero en esa sesión de ventana — si `markdownhelp_*.md` cambiaba en disco (única forma real: durante desarrollo), la ayuda seguía mostrando el contenido viejo hasta recargar toda la ventana. Ahora relee del fichero en cada apertura (`fetch(..., { cache: 'no-store' })`); el parseo/sanitizado/resaltado completo solo se repite si el contenido leído cambió de verdad respecto al ya renderizado, para no pagar ese coste en cada clic sin necesidad.
  - ✅ Verificado 2026-08-18 en Windows lanzando la app real: el modal abre con el contenido correcto según el idioma activo, con encabezados/citas/listas/enlaces renderizados correctamente. Tras el fix: enlace interno de la propia ayuda navega a su sección (antes no funcionaba, mismo bug de ids que RF-04) y un diagrama Mermaid de ejemplo se renderiza como SVG real dentro del modal (antes se mostraba como código sin procesar).
- [x] **RF-23: Barra de formato Markdown en el Modo Edición, añadido 2026-08-19:**
  - Segunda fila de 16 iconos dentro del panel de código (RF-20), visible solo en Modo Edición: negrita, cursiva, tachado, código inline, H1/H2/H3, lista, lista numerada, tarea, enlace, imagen, cita, bloque de código, tabla, línea horizontal. Nace de una fricción real señalada por el usuario probando el Modo Edición: hay que saberse la sintaxis Markdown de memoria, sin ninguna ayuda de inserción (RF-22 solo es de referencia, no interactiva).
  - Con texto seleccionado, envuelve/transforma la selección — y permite alternar: pulsar Negrita dos veces sobre el mismo texto lo desenvuelve, igual con cursiva/tachado/código/encabezados/listas/citas/tareas. Sin selección, inserta un esqueleto mínimo (p. ej. `**texto en negrita**`, una tabla de 3 columnas con 2 filas de ejemplo) con el placeholder relevante ya preseleccionado, listo para sobrescribir.
  - Cada acción termina disparando el mismo evento `input` nativo que ya maneja una edición tecleada — sin caminos divergentes con `isDirty`, el debounce de render ni la gestión de conflictos (RF-21).
  - **Sin resaltado de sintaxis ni componente de editor de código** (coherente con la decisión de RF-20/ADR-027): todos los iconos son SVG inline o glifos de texto (`B`/`I`/`H1`...), sin librería de iconos ni de editor nueva.
  - Atajos de teclado opcionales `Ctrl+B`/`Ctrl+I` cuando el editor tiene el foco, para los dos formatos más frecuentes; el resto de formatos solo tienen botón (no se pidieron atajos adicionales, evita colisiones futuras).
  - **Bug real corregido (encontrado por el usuario probando la barra):** el doble clic del `<textarea>` para seleccionar una palabra incluye el espacio siguiente en la selección — al envolverla tal cual salía `**palabra **`/`[palabra ](url)`, que CommonMark no reconoce como énfasis/enlace válido. Los espacios en los bordes de la selección se recortan antes de aplicar el marcado en vez de envolverlos también.
  - ✅ Verificado 2026-08-19 en Windows: los 16 botones probados en vivo por el usuario y por UI Automation, con y sin selección, incluido el toggle de los formatos simétricos y el fix del doble clic. **No verificado en Linux/macOS** en esta sesión (solo Windows disponible) — riesgo aceptado, mismo criterio que el resto de RF multiplataforma.
- [x] **RF-24: Indentación con Tab/Shift+Tab en el Modo Edición, añadido 2026-08-19:**
  - Dentro del `<textarea>` del panel de código, `Tab` indenta y `Shift+Tab` desindenta la línea actual (o todas las líneas de la selección) en incrementos de 2 espacios, en vez del comportamiento nativo del navegador de mover el foco al siguiente control — lo que antes hacía imposible anidar listas Markdown sin salir del teclado y reposicionar el cursor a mano.
  - El listener está scoped al propio `editorTextarea` (no a `window`), así que no interfiere con la navegación por Tab normal en el resto de la interfaz (buscador, panel de URL, modales).
  - Fuera de alcance de este RF: indentación "inteligente" que detecte el nivel de anidamiento de una lista y ajuste el marcador automáticamente (p. ej. `-` → `*` en el segundo nivel) — se indenta el texto tal cual, coherente con ser un `<textarea>` plano sin comprender la estructura Markdown.
  - ✅ Verificado 2026-08-19 en Windows: indentar/desindentar una línea suelta y una selección multilínea sobre ítems de lista hermanos, ida y vuelta exacta (Tab seguido de Shift+Tab no deja residuo), y confirmado que el foco nunca sale del editor mientras el resto de la app conserva su navegación por Tab habitual. **No verificado en Linux/macOS** en esta sesión — mismo riesgo aceptado que RF-23.
- [x] **RF-25: Explorador de árbol de directorios (panel lateral, estilo VS Code), añadido 2026-08-20:**
  - **Raíz del árbol = carpeta del archivo actualmente abierto.** Al cargar un documento (apertura explícita, navegación por enlaces RF-08A o recarga), el árbol se reconstruye con esa carpeta como raíz — sigue siempre al documento activo, no es una "carpeta de trabajo" fija independiente de él.
  - **Sin archivo abierto (arranque en frío):** se muestra la carpeta del último archivo local abierto (misma fuente que RF-11, Archivos Recientes). Si esa carpeta ya no existe en disco (movida/borrada), el árbol cae al mismo estado vacío que tendría una instalación nueva — sin error visible, mismo criterio de autocuración que `filter_existing` (RF-11).
  - **Árbol expandible multinivel:** carpetas con flecha para expandir/colapsar subcarpetas, leídas bajo demanda al expandir (no se lee recursivamente toda la carpeta de golpe). Un archivo remoto (`http(s)://`, RF-08A) no tiene carpeta local que listar — el árbol se oculta para ese documento, igual que ya ocurre con el Modo Edición (RF-20/RF-21) sobre documentos remotos.
  - **Interacción de clic:** un clic sobre un archivo `.md` lo abre en la ventana actual (sustituye el documento, mismo comportamiento que Recientes/RF-11). `Ctrl + clic` (`Cmd + clic` en macOS) lo abre en una ventana nueva — ver la excepción consciente añadida a RF-14 arriba.
  - **Archivos que no son `.md`:** se listan visibles (carpetas e imágenes/assets incluidos, para reflejar la estructura real de disco) pero **no son clicables** — no amplían el alcance de la app más allá de archivos Markdown.
  - **Arrastrar una carpeta sobre la ventana** (ampliación del Drag & Drop de RF-09, que hoy solo acepta un archivo `.md`) fija esa carpeta como raíz del árbol sin necesidad de abrir antes ningún archivo dentro de ella — vía útil para el caso de arranque en frío sin depender de Recientes.
  - **Filtro de texto** en la cabecera del árbol: un campo de texto oculta en tiempo real (sin llamada a Rust adicional, sobre los nodos ya cargados en memoria) las carpetas/archivos cuyo nombre no contenga el texto tecleado.
  - **Ubicación:** nueva pestaña "Archivos" dentro del panel lateral derecho ya existente (mismo `#toc-sidebar` redimensionable de RF-04), junto a una pestaña "Índice" que pasa a alojar la Tabla de Contenidos de encabezados ya existente — no se añade un tercer panel en pantalla, ver ADR-029 en `memory.md`.
  - **Solo navegación de solo lectura:** no permite crear, renombrar, mover ni borrar archivos o carpetas desde el árbol (ver sección 6, Fuera de Alcance).
  - **"Revelar en el Explorador de archivos" (menú contextual, clic derecho):** abre el gestor de archivos nativo del sistema operativo con el archivo/carpeta seleccionado ya resaltado — vía de escape hacia el sistema operativo para lo que el árbol no cubre a propósito (crear, renombrar, mover, borrar; ver punto anterior). Comando Rust nuevo `reveal_in_file_manager(path)`, sin plugin oficial ni dependencia nueva (`std::process::Command`, mismo criterio que el resto de comandos propios): `explorer /select,<path>` en Windows, `open -R <path>` en macOS. **Linux, alcance reducido:** no existe un comando universal de "seleccionar archivo exacto" entre gestores de archivos (Nautilus, Dolphin...) — se abre la carpeta contenedora con `xdg-open` sin selección, documentado como limitación conocida de la plataforma (mismo criterio que otras diferencias de la tabla en `ARCHITECTURE.md` §5).
  - **"Abrir en ventana nueva" (menú contextual, añadido 2026-08-20 a petición del usuario tras probar `Ctrl+clic` en real):** segunda opción del mismo menú contextual, solo visible sobre archivos `.md` (no sobre carpetas ni no-`.md`), con el atajo de teclado indicado en la propia etiqueta (`Ctrl+clic`/`Cmd+clic` según plataforma) para quien prefiera el menú al atajo o quiera confirmarlo. Invoca el mismo comando `open_in_new_window` que ya usa el modificador de clic — ver el bug real encontrado y corregido en `memory.md` (ventana en blanco/colgada, causa: reentrancia de `run_on_main_thread` al invocarse ya desde el hilo principal dentro del propio despacho IPC).
  - **Android (añadido 2026-09-02):** la raíz del árbol es la carpeta concedida vía SAF (`ACTION_OPEN_DOCUMENT_TREE`, con `takePersistableUriPermission` para que el permiso sobreviva a reinicios de la app) en vez de la carpeta del último archivo abierto — el usuario la elige explícitamente una vez y la app la recuerda. La lectura de cada nivel usa `DocumentsContract.buildChildDocumentsUriUsingTree()` en vez de `std::fs::read_dir`. **"Abrir en ventana nueva" no aplica** (ver RF-14 en Android) — un toque simple siempre sustituye el documento actual, sin equivalente táctil a `Ctrl/Cmd+clic`. **"Revelar en el Explorador de archivos" tampoco aplica** — Android no tiene un gestor de archivos del sistema accesible por Intent estándar con selección de archivo exacta.
- [x] **RF-26: Selector rápido de archivos (Quick Open, `Ctrl/Cmd + K`), añadido 2026-08-20:**
  - Cuadro de búsqueda flotante (mismo patrón visual que el buscador `Ctrl+F` de RF-04 y el panel "Abrir desde URL" de RF-08A-UI) que filtra en tiempo real por nombre entre los archivos `.md` ya cargados en el árbol (RF-25) de la carpeta del documento activo — misma fuente de datos, sin comando Rust adicional propio.
  - `Enter` sobre el resultado resaltado (o clic) abre el archivo en la ventana actual; `Ctrl/Cmd + Enter` lo abre en una ventana nueva — mismo criterio de modificador que el árbol (RF-25), para no introducir una segunda convención de atajo en la misma versión.
  - Navegación de resultados con flechas arriba/abajo, `Escape` cierra el panel sin abrir nada (mismo patrón que el resto de paneles flotantes, `registerPanel()` — ver ADR-011 en `memory.md`).
  - **Elección de atajo (`Ctrl/Cmd+K` en vez del `Ctrl+P` habitual de VS Code):** `Ctrl+P` ya está tomado por Imprimir/PDF (RF-10) en esta app — se adopta `Ctrl/Cmd+K`, la convención de "quick switcher" ya establecida en Slack, Discord, GitHub.com y Linear, y libre de colisión con el resto de atajos existentes (`Ctrl+E/F/O/P/S`). Decisión tomada explícitamente con el usuario en sesión de brainstorming (2026-08-20).
  - Sin resultados si la carpeta del documento activo no tiene ningún otro `.md` — mensaje discreto, no un estado de error.
  - **Android (añadido 2026-09-02):** mismo comportamiento sobre los nodos ya cargados del árbol SAF (RF-25); sin variante "abrir en ventana nueva" por el mismo motivo que RF-25 en Android — un toque o Enter siempre sustituye el documento actual.
- [x] **RF-27: Exportar documento a Typst (`.typ`), implementado 2026-09-19 (ciclo v0.16.0):**
  - Nuevo botón "Exportar a Typst" (barra superior, junto a Imprimir/RF-10) que convierte el Markdown crudo del documento actualmente abierto a sintaxis Typst y lo guarda como fichero `.typ` junto al original (o donde el usuario elija, vía el diálogo nativo de guardado ya usado por otras exportaciones).
  - **Conversor propio, sin Pandoc ni dependencia externa pesada** (coherente con los presupuestos de tamaño de instalador/RAM, sección 5): recorre los tokens ya generados por `markdown-it` (el mismo árbol que alimenta el renderizado HTML, RF-02) y los traduce a construcciones Typst equivalentes — encabezados (`=`/`==`/`===`), negrita/cursiva/tachado, listas con y sin viñetas, bloques e inline de código (` ```lang ` → ` ```lang `, ` `` ` inline), tablas (`#table(...)`), imágenes (`#image("...")`) y enlaces (`#link("url")[texto]`).
  - **Fuera de alcance de este RF** (ver sección 6): fórmulas LaTeX (RF-17), diagramas Mermaid (RF-15) y alertas GFM (RF-02) — construcciones sin equivalente 1:1 sencillo en Typst; se exportan como código (`#raw(...)`) sin traducir (mejor un resultado explícitamente "sin convertir" que una traducción incorrecta silenciosa) hasta que se decida un tratamiento específico.  
    **🔧 Bug real corregido en el pase Bugs de `/code-simplify` (ciclo v0.16.0):** las fórmulas (RF-17) no llegaban siquiera a salir como "código sin traducir" — el placeholder HTML interno que usa `extractMath()` para no romper el parseo de `markdown-it` (`<span class="dbv-math" data-i="N">`) se descartaba junto con el resto del HTML embebido genérico (`html_inline`), borrando la fórmula del `.typ` sin dejar rastro. `typstInline()` ahora reconoce específicamente ese placeholder (vía el mismo array `formulas` que ya devuelve `extractMath()`) y lo sustituye por el LaTeX original envuelto en `#raw(...)`, en vez de descartarlo con el resto del HTML genérico.  
    **🔧 Segundo bug real corregido en el mismo pase — atajos de símbolo de Typst (`~`, `--`, `---`, `...`) en prosa normal:** además de los caracteres sueltos con significado propio (`#`, `$`, `_`...), Typst interpreta ciertas *secuencias* en modo markup como atajos de otros glifos (`~` → espacio irrompible, `--`/`---` → guion en/em, `...` → puntos suspensivos — ver `typst.app/docs/reference/symbols/#shorthands`). Un documento real con una ruta (`~/proyecto`), un guion largo tecleado a mano (`---`) o unos puntos suspensivos sueltos perdía esos caracteres silenciosamente al exportar, sustituidos por otro glifo sin ningún aviso. `escapeTypstText()` ahora también escapa el primer carácter de cada una de estas secuencias (verificado con el compilador real que basta con escapar solo el primero para desactivar toda la secuencia, sin dejar residuo interpretable).
    **🔧 Tercer bug real, reportado por un usuario (Johannes Rexx) en el foro de Typst:** el HTML embebido en el documento (`html_inline`/`html_block` de `markdown-it`) también entra en la categoría "fuera de alcance, sin traducir" — pero se estaba descartando en silencio en vez de salir como código sin traducir: una etiqueta suelta como `<module>` (habitual citando la traza de un error) desaparecía de una cita en bloque, y un bloque `<div>...</div>` sin fence desaparecía por completo, ya que `markdownToTypst()` no tenía ninguna rama para `html_block`. Ahora el HTML inline sale como texto literal escapado y el HTML en bloque como `#raw(..., block: true)`, igual que el resto de construcciones "fuera de alcance" de este RF.
  - No requiere tener `typst` (el compilador) instalado — la app solo genera el fichero fuente `.typ`; compilarlo a PDF queda en manos del usuario o de una herramienta externa como `dbv-typst-editor`.
  - **Nota de alcance del ciclo:** la funcionalidad complementaria "Importar Markdown" en `dbv-typst-editor` (el otro proyecto del usuario) se especifica y planifica por separado, en ese repositorio — fuera del `/build` de este ciclo.
  - **Implementación:** botón nuevo en la barra superior (junto a Imprimir), `markdownToTypst()` + `typstInline()` + `escapeTypstText()`/`escapeTypstString()` en `app.js`, y comando Rust `save_typst_dialog` (diálogo nativo "guardar como" con filtro `*.typ`, análogo a `open_file_dialog`); la escritura reutiliza `write_file` (RF-20), sin comando de escritura nuevo. Cobertura: encabezados, negrita/cursiva/tachado, código, listas con y sin viñetas (anidadas por sangría), citas (`#quote(block: true)[...]`), tablas (`#table(columns: N, ...)`), imágenes (`#image`, o `#link` si la URL es remota — Typst no soporta imágenes por red), enlaces (`#link`) y líneas horizontales (`#line`).
  - **Promoción cruzada, pedida por el usuario 2026-09-19:** la segunda línea del `.typ` generado es un comentario `//` recomendando `dbv-typst-editor` (el otro proyecto del usuario) para abrir el fichero exportado, en el idioma activo de la interfaz (clave `typst.editorPromo` en `i18n.js`, ES/EN, mismo criterio que RF-16).
  - **Código (inline y bloque) vía `#raw()`, no delimitadores de backticks (corregido tras verificación en real, ver Lección en `memory.md`):** el primer intento usaba backticks contando la racha más larga de comillas internas para elegir el delimitador — la convención de CommonMark. **El usuario probó la exportación de `GFM_test.md` en su propio editor de Typst y encontró un fallo de compilación real** (`unclosed raw text`, que arrastraba todo el resto del documento a interpretarse como marcado normal — de ahí `__init__` de Python leído como énfasis vacío). Typst no sigue la regla de CommonMark: un delimitador de 2 backticks con una sola comilla dentro seguía sin cerrar, y 3+ backticks en la misma línea que el contenido se interpreta como apertura de bloque, no como inline. Corregido usando `#raw("contenido", lang: "...", block: true)` — una cadena Typst normal, inmune a cualquier racha de backticks internas; solo necesita escapar `\` y `"` (`escapeTypstString()`).
  - ✅ **Verificado 2026-09-19 con el compilador real (`typst 0.15.1`), en dos rondas:** (1) un documento de prueba con los caracteres especiales de Typst en prosa normal (`#5`, `$10`, `@usuario`, `[referencia]`) compila limpio, con el escapado confirmado en la salida (`\#5`, `\$10`...). (2) **`GFM_test.md` completo (57 secciones, 878 líneas de salida) compila limpio, sin errores ni avisos**, tras localizar la causa raíz del fallo reportado por el usuario con una bisección binaria sobre los tokens de `markdown-it` (banco de pruebas en `scratchpad/`, mismo criterio que Lección 26) — incluye el caso adversarial real que lo disparó, `` ``code ` with backtick`` `` de la sección 39.
- [ ] **RF-28: Exportar PDF dedicado, sin diálogo nativo de impresión (en investigación, ciclo v0.16.0):**
  - Objetivo: sustituir o complementar el `window.print()` actual de RF-10 (que depende del diálogo de impresión del sistema operativo, y de que el usuario marque "gráficos de fondo" a mano para no perder colores/fondos) por un botón "Exportar PDF" que genere el fichero directamente.
  - **Spike técnico completado 2026-09-19 → POSPUESTO a v0.17.0 (o descartado), a decidir por el usuario.** Hallazgos:
    - **Es técnicamente posible en las tres plataformas, pero solo con código específico de cada una:** en Windows, `ICoreWebView2_7::PrintToPdf` vía `webview2-com` (ya presente en el árbol de dependencias de forma transitiva a través de `wry 0.55.1`, así que no añadiría peso al instalador) accediendo al webview nativo con `WebviewWindow::with_webview()`; en macOS, `WKWebView.createPDF` vía enlaces ObjC; en Linux, `webkit_print_operation` con salida a fichero. No existe ninguna API multiplataforma de Tauri v2 para esto (`webview.print()` siempre abre el diálogo del sistema).
    - **Coste:** tres implementaciones distintas con COM/ObjC/GTK en crudo (`unsafe`), rompiendo la propiedad —hoy cierta salvo para el menú de macOS y Android— de que `src-tauri/src/lib.rs` es 100 % multiplataforma. Solo una de las tres (Windows) sería verificable en el entorno de desarrollo actual.
    - **La justificación original desapareció:** RF-28 nació porque al exportar a PDF "se perdían los colores" y había que marcar "gráficos de fondo" a mano en el diálogo del sistema. Eso era un bug propio de `styles.css`, corregido y verificado en este mismo ciclo (ver RF-10). Lo que queda es conveniencia (ahorrar un diálogo), no corrección.
    - **Recomendación:** no implementarlo en v0.16.0. Si se retoma, hacerlo como RF con su propio `/spec`, valorando además que un botón que en Windows guarda directo y en macOS/Linux abre un diálogo sería una divergencia de comportamiento visible para el usuario.

- **Core / Backend:** **Rust** + **Tauri v2**.
- **Sanitizador de Seguridad:** **DOMPurify** (JS, frontend) sobre el HTML ya renderizado por `markdown-it` — decisión actualizada 2026-08-09 (ver ADR-009 en `memory.md`); reemplaza la idea original de `ammonia` en Rust, que operaría sobre Markdown crudo y corrompería bloques de código.
- **File Watcher:** **Notify crate** (Rust) para la recarga en vivo de archivos editados, vigilando el directorio padre (ADR-010) para sobrevivir a guardados atómicos.
- **HTTP Client (Remotos):** **ureq** (Rust, bloqueante, sin runtime async) para descargar documentos Markdown remotos si el usuario hace clic en una URL `.md`.
- **Frontend / WebView:** HTML5, CSS3 (Tailwind CSS), JavaScript Vanilla (modular).
- **Historial de Navegación:** Pila de navegación (Stack) en JS/Rust con soporte para enlaces relativos al directorio del archivo actual.
- **Parser de Markdown:** **markdown-it** (CommonMark + soporte de HTML integrado).
- **Resaltado de Sintaxis:** **Prism.js**.
- **Diagramas:** **mermaid.js** (generación de SVG vectorial).
- **Matemáticas:** **KaTeX** (renderizado LaTeX síncrono, vendorizado localmente sin CDN — RF-17).
- **Edición (RF-20/RF-21, añadido 2026-08-18):** sin librería de editor de código — `<textarea>` plano reutilizando el mismo pipeline de renderizado (`markdown-it` + DOMPurify) que el modo lectura. Nuevo comando Rust `write_file` (escritura directa, mismo patrón sin plugin `fs` oficial que `read_file`).
- **Explorador de árbol y Quick Open (RF-25/RF-26, planificado 2026-08-20):** sin librería de árbol/componente UI nueva — SVG inline y CSS propio, mismo patrón vanilla que la Tabla de Contenidos (RF-04). Nuevo comando Rust de solo lectura sobre `std::fs::read_dir` (parte de `std`, sin dependencia nueva) para listar el contenido de una carpeta bajo demanda; sin plugin `fs` oficial de Tauri, mismo criterio ya establecido para `read_file`/`write_file`.
- **Android (RF-01/06/07/08A/25/26, planificado 2026-09-02):** Storage Access Framework (SAF) en vez de `std::fs` para todo acceso a archivos — `DocumentsContract`/`ContentResolver` sobre URIs `content://`, vía el plugin de Tauri para Android o un plugin propio en Kotlin, expuesto al mismo frontend JS. Sin `notify` (RF-06), sin `tauri-plugin-single-instance` (RF-14), sin `tauri-plugin-updater` (RF-13, sustituido por Google Play). Ver ARCHITECTURE.md §5 para el detalle de gating de código exclusivo de escritorio.
- **Exportar a Typst (RF-27, planificado 2026-09-19):** sin dependencia nueva — función propia en `app.js` que recorre los tokens de `markdown-it` ya generados para RF-02, sin invocar a Rust (misma filosofía que el resto de exportaciones/utilidades de solo-frontend).
- **Distribución macOS vía Homebrew (planificado 2026-09-19):** tap propio `davidbuenov/homebrew-dbv-md-reader` (repositorio GitHub nuevo, ver RNF-01) con un `Cask` (no `Formula` — es una app GUI empaquetada en `.dmg`, no un binario CLI) apuntando al `.dmg` universal ya publicado por `release-macos.yml`. Actualización automática del Cask (versión + sha256) como paso añadido al final de ese mismo workflow tras publicar cada Release, sin proceso manual.

---

## 📐 5. Requisitos No Funcionales

- **Tamaño del Instalador:** Menor a 20 MB (`.msi` o `.exe` para Windows). *(Relajado el 2026-08-09 de <8 MB a <20 MB — decisión consciente del usuario: prioriza la funcionalidad completa (seguridad, auto-reload, imágenes, remotos) sobre el tamaño mínimo. Sigue siendo drásticamente inferior a una app Electron equivalente.)* *(Confirmado 2026-08-18 que RF-20/RF-21 no requieren relajar este presupuesto — sin librería de editor de código nueva, ver ADR-027 en `memory.md`.)*
- **Consumo de RAM:** Menor a 64 MB en reposo con un documento abierto.
- **Tiempo de Respuesta:** Renderizado inicial completado en menos de 200 ms tras abrir el archivo.
- **Soporte de Plataforma (RNF-01, 2026-08-12):**
  - **Windows (cambio de política, 2026-09-19):** **canal único desde `v0.16.0`: Microsoft Store (MSIX).** El instalador NSIS firmado en GitHub Releases queda descontinuado a partir de esta versión — decisión explícita del usuario, motivada por la pérdida de acceso a la contraseña de la clave de firma `minisign` (`~/.tauri-keys/dbv-md-reader.key`, guardada en un gestor de contraseñas antes de que se prohibiera tener secretos en las conversaciones) y por el buen funcionamiento ya demostrado del canal Store (actualizaciones rápidas, cero fricción de firma para el usuario). Mismo criterio que Android (RNF-01 más abajo): **una única tienda oficial por plataforma, sin réplica en GitHub Releases.** El `.exe`/`.msixbundle` de `v0.16.0` se generaron **sin firmar** (sin `TAURI_SIGNING_PRIVATE_KEY*` en el entorno) — no se genera `.sig` ni se publica `latest.json` nuevo, así que RF-13 ("Buscar actualizaciones") deja de encontrar versiones nuevas para quien ya tuviera instalado el canal NSIS; esos usuarios deben migrar a la Store manualmente una vez. **Pendiente, fuera del alcance de Claude Code:** recuperar o rotar la clave de firma si en el futuro se decide reabrir el canal NSIS (rotarla invalida el auto-update de todas las instalaciones NSIS ya existentes, ver ADR pendiente si se retoma), y completar la submission del `.msixbundle` de `v0.16.0` a Partner Center.
  - **Linux:** canal oficial vía CI (GitHub Actions) — `.deb` y `.AppImage` publicados como Release de GitHub en cada tag `vX.Y.Z`. **Sin auto-actualización** en esta fase (RF-13 queda fuera de alcance para Linux — ver ADR-019 en `memory.md`); el usuario descarga manualmente cada versión nueva.
  - **macOS:** **sin canal oficial de firma/notarización** — no se publica ningún binario firmado (decisión consciente: evitar el coste de la cuenta Apple Developer, 99 $/año). El `.dmg` universal sin firmar de `release-macos.yml` sí se publica como asset de GitHub Release (ver ARCHITECTURE.md §5); se documentan además instrucciones de auto-compilación (`cargo tauri build`) en `README.md` para quien prefiera compilar desde el código.  
    **Homebrew (planificado 2026-09-19, ciclo v0.16.0):** canal de instalación adicional vía tap propio `davidbuenov/homebrew-dbv-md-reader` (`brew tap` + `brew install --cask dbv-md-reader`), consumiendo ese mismo `.dmg` sin firmar — no sustituye la limitación de no estar firmado/notarizado (Gatekeeper seguirá pidiendo autorización la primera vez), solo simplifica la instalación/actualización frente a descargar el `.dmg` a mano. El Cask se actualiza automáticamente (versión + sha256) como último paso de `release-macos.yml` en cada Release nueva, sin intervención manual del maintainer.
  - **Android (añadido 2026-09-02, ver ADR-031/033 en `memory.md`):** canal oficial **Google Play Store** desde el primer lanzamiento (decisión explícita del usuario, distinta del criterio "validar primero fuera de tienda" ya usado en Linux) — requiere cuenta de desarrollador de Google Play y política de privacidad publicada (obligatoria en Play Console para cualquier app, no solo las que piden permisos sensibles). **MVP de solo lectura**: RF-20/RF-21 (Modo Edición) fuera de alcance en esta fase. Sin auto-actualización propia (RF-13 fuera de alcance, gestionado por Play). Objetivo de RAM/tamaño de APK pendiente de medir en el primer build real — sin presupuesto numérico fijado todavía, a diferencia de Windows/Linux/macOS.

---

## 🚫 6. Fuera de Alcance (Out of Scope)

- [x] ~~Edición o modificación del contenido de los archivos `.md` (aplicación strictly de solo lectura).~~ **Revertido 2026-08-18** — ver RF-20/RF-21 y ADR-027 en `memory.md`.
- [ ] Edición colaborativa en tiempo real (varios usuarios sobre el mismo archivo) o fusión automática (merge) de cambios divergentes (ver RF-21).
- [ ] Editor de código completo: resaltado de sintaxis en el panel de edición, autocompletado, snippets, control de versiones/histórico de cambios o linter Markdown (ver RF-20).
- [ ] "Guardar como" / crear un archivo `.md` nuevo desde cero dentro del editor (RF-20 solo sobreescribe el archivo ya abierto).
- [ ] Base de datos pesada de notas o sincronización en la nube propietaria.
- [ ] Crear, renombrar, mover o borrar archivos/carpetas desde el Explorador de árbol (RF-25) — navegación de solo lectura únicamente.
- [ ] Abrir archivos que no son `.md` desde el árbol (imágenes, PDFs, etc.) con la aplicación por defecto del sistema — se listan visibles pero no son clicables (RF-25).
- [ ] Modo Edición (RF-20/RF-21) en Android en esta primera fase — MVP de solo lectura, ver RNF-01 y ADR-031 en `memory.md`.
- [ ] Recarga en vivo por cambios externos (RF-06) en Android — limitación de Storage Access Framework, ver RF-06.
- [ ] "Abrir en ventana nueva" y "Revelar en el Explorador de archivos" (RF-25) en Android — sin equivalente en el modelo de actividad única/táctil, ver RF-14 y RF-25 en Android.

---

## ⚠️ 7. Riesgos y Mitigación

- **Riesgo:** Inyección de código malicioso XSS mediante HTML embebido en archivos Markdown no confiables (locales o remotos).  
  - **Mitigación:** Sanitización estricta con DOMPurify (JS) sobre el HTML ya renderizado por `markdown-it`, antes de insertarlo en el DOM del WebView (ver RF-03 y ADR-009 en `memory.md`).  
- **Riesgo:** Bloqueo de WebView2 al intentar cargar archivos locales mediante `file://`.  
  - **Mitigación:** Uso del protocolo `asset://` de Tauri para la resolución de imágenes y documentos `.md` locales relativos.  

---

## ❓ 8. Preguntas Abiertas

- [x] **Stack:** ¿Estrategia A (Rust + Tauri v2 + WebView2)? Confirmado.
- [x] **Nombre del proyecto:** `dbv-md-reader` (Confirmado por el usuario).
- [x] **Navegación Cursos / Docs:** Soporte total de navegación entre archivos `.md` locales enlazados con historial de Atrás (`Alt + ←`) / Adelante (`Alt + →`). Confirmado.
- [x] **Presupuesto de tamaño vs. RF-03/06/08A:** Confirmado por el usuario (2026-08-09): se relaja el NFR de tamaño a <20 MB y se implementan todos los RF pendientes (RF-03, RF-06, RF-07, RF-08A, RF-11) en este ciclo sin recortar funcionalidad.
- [x] **Alcance MVP Android:** ¿Solo lectura o lectura + edición desde el día 1? Confirmado por el usuario (2026-09-02): **solo lectura** — RF-20/RF-21 fuera de alcance en esta fase.
- [x] **Modelo de archivos en Android:** ¿Árbol completo vía SAF o solo apertura de archivo único? Confirmado (2026-09-02): **árbol completo vía SAF** (`ACTION_OPEN_DOCUMENT_TREE`), manteniendo el Explorador de árbol (RF-25) y Quick Open (RF-26) como en escritorio.
- [x] **Recarga en vivo (RF-06) en Android:** ¿Fuera de alcance o polling periódico? Confirmado (2026-09-02): **fuera de alcance** — ver RF-06 en Android.
- [x] **Canal de distribución Android:** ¿GitHub Releases (APK sideload) o Google Play desde el día 1? Confirmado (2026-09-02): **Google Play Store desde el día 1** — ver RNF-01.
- [x] **Ubicación del conversor Markdown → Typst:** ¿En `dbv-md-reader` (exportar), en `dbv-typst-editor` (importar), o ambos? Confirmado por el usuario (2026-09-19): **ambos**, pero como tareas independientes — este ciclo (`v0.16.0`) solo cubre la exportación aquí, en RF-27; la importación en `dbv-typst-editor` se especifica y planifica por separado en ese repositorio.
- [x] **Alcance del fix de impresión/PDF:** ¿Solo el bug de color (RF-10), o valorar también un "Exportar PDF" dedicado sin diálogo nativo? Confirmado por el usuario (2026-09-19): **ambos** — el fix de color entra sí o sí en `v0.16.0` (RF-10); el "Exportar PDF" dedicado (RF-28) se investiga en este mismo ciclo pero puede posponerse a `v0.17.0` si el spike técnico no encuentra una vía limpia dentro del presupuesto de tamaño/RAM del proyecto.
- [x] **Automatización del tap de Homebrew:** ¿Actualización manual del Cask tras cada Release, o automática desde CI? Confirmado por el usuario (2026-09-19): **automática** — paso añadido al final de `release-macos.yml` que actualiza el Cask (versión + sha256) en el repo nuevo `davidbuenov/homebrew-dbv-md-reader`, sin intervención manual del maintainer en cada Release.
